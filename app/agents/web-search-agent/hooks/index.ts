export { useWebSearch, WebSearchProvider } from '../context/WebSearchContext';

// Re-export types that might be needed by consumers
export type { WebSearchResult, WebSearchSettings, WebSearchResponse } from '../types'; 